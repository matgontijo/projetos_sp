services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: custeio
      POSTGRES_PASSWORD: custeio
      POSTGRES_DB: custeio
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U custeio"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  pgdata:
